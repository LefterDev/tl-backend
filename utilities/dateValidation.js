// const Booking = require("../Schemas/Booking");

// const validateDate = (start, end) => {
//   const currDate = new Date().getTime();
//   if (start <= currDate || start >= end) return false;
//   return true;
// };

// const bookedDate = async (start) => {
//   const queury = await Booking.findOne({ bookDate: start });
//   if (queury == null) return false;
//   return true;
// };

// module.exports = { validateDate, bookedDate };
